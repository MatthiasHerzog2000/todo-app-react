export class Appointment {
    constructor(public id : string, public firstname : string, public lastname : string, public email : string, public date : Date, public title : string, public description : string) {}
}